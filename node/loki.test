const { createLogger, transports } = require("winston");
const LokiTransport = require("winston-loki");
const options = {
  transports: [
    new LokiTransport({
      json: true,
      host: "https://grafana-agent-equinsuocha.cloud.okteto.net",
      onConnectionError: (err) => {
        console.log(err)
      }
    })
  ]
};
const logger = createLogger(options);

logger.info({message: 'test', labels: {one: 'two'}});